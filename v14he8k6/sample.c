#include<stdio.h>
main()
{
  static int i,j,a[i][j],b[i][j],*d,k,l,m,n;
  printf("Enter order of a ");
  scanf("%d %d",k,l);
  printf("Enter order ");
  scanf("%d %d",m,n);
  d=addition(a,b);
  printf("Addition of matrices is:");
   for(i=0;i!='\0';i++)
    {

    }

}
  void addition(int *p,int *q)
   {
     int i,j,c[i][j],n,*a;
       for(i=0;i<)
      for(i=0;i<m;i++)
      {
       for(j=0;j<n;j++)
        {
           c[i][j]=;
        }
     } 

     return(c);
   }
