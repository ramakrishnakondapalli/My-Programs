












char* atof(const char*str)
{

   for(i=0;i<strlen(str);i++)
        if(str[i]=='.')
           i++;
           for(j=0;j<strlen(str)-i;j++)
                 str1[j]=str[i+j]

             m=strlen(str);
             n=strlen(str1);
         while(num/10!=0)
            sum=sum+rem*str[n-i-1];
               rem=rem*10;




    num=num*10+(str(i++)-'0');//   0*10+7
                              //   7*10+5
                              //    75*10+4
}
