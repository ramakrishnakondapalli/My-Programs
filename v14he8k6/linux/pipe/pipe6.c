#include"headers.h"
main()
{
   int p[2];
    int fd1,fd2,v[100];
    pipe(p);
     fd1=open("f1",O_RDWR);
     while((fd1,v,4)==1);
      fd2=open("f2",O_WRONLY);
      while(write(fd2,v,4)==1);
          
   
}
